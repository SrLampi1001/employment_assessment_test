def test_pytest_collects() -> None:
    """Phase 0 smoke test — confirms the test harness boots cleanly.

    This is the only test in Phase 0. Real coverage starts in Phase 1 with
    the two mandatory BDD scenarios from ARCHITECTURE.md §10.
    """
    assert True
